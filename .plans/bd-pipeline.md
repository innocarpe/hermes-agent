# BD Pipeline Foundation Implementation Plan

> Obsolete plan kept only as a pointer.

This plan originally assumed TemperStone HQ business documents would live inside the Hermes repo.

That assumption is wrong.

Canonical location:
- `/Users/WooseongKim/Projects/Temperstone/temperstone-hq`

Rules:
- Do not recreate any TemperStone HQ document folder inside `hermes-agent`.
- Do not add document-existence tests in `hermes-agent/tests/docs` for TemperStone HQ files.
- Any future BD / HQ document work must be written directly in the TemperStone HQ repo.
